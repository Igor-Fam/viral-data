import { StyleSheet } from "react-native";

export default StyleSheet.create({
    chart: {
        borderColor: '#c2c2c2',
        borderRadius: 15,
        borderWidth: 1,
    },
    main: {
        alignItems: 'center',
    },
    sliderbox: {
        borderRadius: 15,
        backgroundColor: '#e0e0e0',
        width: '80%',
        marginTop: 20,
        paddingTop: 20,
        paddingBottom: 20,
        alignItems: 'center',
    }
});