import { StyleSheet } from "react-native";

export default StyleSheet.create({
    chart: {
        borderColor: '#c2c2c2',
        borderRadius: 15,
        borderWidth: 1,
        marginTop: 20,
    },
});

